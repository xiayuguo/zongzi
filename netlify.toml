# Netlify 部署配置
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA 友好：任何路径回退到 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
